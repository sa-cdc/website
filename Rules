#!/usr/bin/env ruby

# A few helpful tips about the Rules file:
#
# * The string given to #compile and #route are matching patterns for
#   identifiers--not for paths. Therefore, you can’t match on extension.
#
# * The order of rules is important: for each item, only the first matching
#   rule is applied.
#
# * Item identifiers start and end with a slash (e.g. “/about/” for the file
#   “content/about.html”). To select all children, grandchildren, … of an
#   item, use the pattern “/about/*/”; “/about/*” will also select the parent,
#   because “*” matches zero or more characters.
#
# * Files located in /static/ are special, the extension is not auto stripped

#compile '/clinic_info/downloads/*/', :rep => :thumbnail do
#  filter :thumbnailize, :width => 42, :default => @items['/static/imgs/pdf_thumbnail.jpg']
#end

#route '/clinic_info/downloads/*/', :rep => :thumbnail do
#  item.identifier.chop + '-thumbnail.png'
#end

compile '/static/test/*/' do
  filter :vanco_url
end

route '/static/test/' do
  '/static/test.txt'
end

passthrough '/static/*'

compile '/stylesheet/' do
  # don’t filter or layout
end

compile '/gala2016/' do
end

compile '/bootstrap/*' do
  # don’t filter or layout
end

compile '*' do
  if item.binary?
    # don’t filter binary items
  else
    filter :erb
    layout 'default'
  end
end

route '/stylesheet/' do
  '/style.css'
end

route '/gala2016/' do
  item.identifier + 'index.html'
end

route '/bootstrap/*' do
  #item.identifier[5..-2]
end

route '*' do
  if item.binary?
    # Write item with identifier /foo/ to /foo.ext
    item.identifier.chop + '.' + item[:extension]
  else
    # Write item with identifier /foo/ to /foo/index.html
    item.identifier + 'index.html'
  end
end

layout '*', :erb
